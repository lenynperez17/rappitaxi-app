name: rapiteam
description: RapiTeam - Tu viaje, tu precio
version: 1.3.0+56

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  
  # State Management
  provider: ^6.1.5+1
  
  # Maps & Location
  google_maps_flutter: ^2.14.0
  google_maps_flutter_web: ^0.5.14+3
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  flutter_typeahead: ^5.2.0  # ✅ NUEVO: Autocomplete sin recrear TextField (resuelve problema de borrado del teclado)
  flutter_polyline_points: ^3.1.0  # Para obtener rutas reales con Google Directions API
  
  # UI & Design
  google_fonts: ^6.2.1
  fl_chart: ^1.1.0
  flutter_rating_bar: ^4.0.1
  percent_indicator: ^4.2.3
  confetti: ^0.8.0  # Animaciones de confeti para celebraciones
  
  # Storage
  shared_preferences: ^2.3.4
  
  # Firebase (actualizadas 2025-12-30)
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  firebase_storage: ^13.0.5
  firebase_messaging: ^16.1.0
  firebase_analytics: ^12.1.0
  firebase_app_check: ^0.4.1+3
  firebase_crashlytics: ^5.0.6
  firebase_database: ^12.1.1
  cloud_functions: ^6.0.5
  
  # OAuth y Autenticación Social
  google_sign_in: ^7.2.0
  crypto: ^3.0.6  # SHA256 para hash de datos sensibles (telefono, etc.)
  
  # Notifications
  flutter_local_notifications: ^19.5.0
  timezone: ^0.10.1
  
  # QR Code
  qr_flutter: ^4.1.0
  
  # Emergency System Dependencies
  flutter_phone_direct_caller: ^2.1.1
  # telephony: ^0.2.0  # DESHABILITADO TEMPORALMENTE PARA BUILD (namespace issue, discontinued)
  # sms_advanced: ^1.0.4  # TAMBIÉN CON NAMESPACE ISSUE - SMS se maneja por backend
  # flutter_sound: ^9.2.13  # REMOVIDO: No implementado y genera warnings de APIs deprecadas
  # audio_session: ^0.2.2    # REMOVIDO: Dependencia de flutter_sound, no usado
  # contacts_service: ^0.6.3  # DESHABILITADO TEMPORALMENTE PARA BUILD (namespace issue)
  audioplayers: ^6.1.0
  
  # Utilities
  intl: ^0.20.1
  url_launcher: ^6.3.1
  uuid: ^4.5.2
  logger: ^2.5.0
  http: ^1.6.0
  webview_flutter: ^4.10.0  # ✅ NUEVO: WebView para MercadoPago Checkout Bricks in-app
  flutter_dotenv: ^5.2.1  # ✅ NUEVO: Gestión segura de variables de entorno desde .env
  
  # ❌ REMOVIDO POR SEGURIDAD: googleapis_auth ya no es necesario
  # Las notificaciones ahora se envían desde Cloud Functions
  dio: ^5.6.0
  
  # OTP y Verificación
  pin_code_fields: ^8.0.1
  flutter_otp_text_field: ^1.2.0
  email_validator: ^3.0.0
  
  # Permissions
  permission_handler: ^12.0.1
  
  # Image - Usa Android Photo Picker en Android 13+ (sin permisos)
  image_picker: ^1.2.1
  
  # File Handling
  file_picker: ^10.3.3
  device_info_plus: ^12.1.0
  cached_network_image: ^3.4.1
  path_provider: ^2.1.5

  # Export & Document Generation
  share_plus: ^10.1.4
  pdf: ^3.11.3
  printing: ^5.13.4
  csv: ^6.0.0

  # Socket.IO para tracking en tiempo real
  socket_io_client: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.2
  flutter_native_splash: ^2.4.3

flutter:
  uses-material-design: true
  generate: true  # ✅ Habilita generación automática de código de localización

  assets:
    - assets/images/
    - assets/images/markers/
    - assets/icons/
    - assets/animations/
    # ❌ REMOVIDO POR SEGURIDAD: service-account.json NO debe estar en el APK
    # Las notificaciones ahora se envían desde Cloud Functions
    - assets/fonts/
    - .env

  # ✅ Configuración de fuentes custom para PDF (Roboto con soporte Unicode completo)
  fonts:
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Regular.ttf
        - asset: assets/fonts/Roboto-Bold.ttf
          weight: 700

# Configuración de íconos de la app
flutter_launcher_icons:
  android: true
  ios: false  # Deshabilitado porque el proyecto no tiene estructura iOS
  image_path: "assets/images/logo_rapiteam.png"
  min_sdk_android: 21
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/logo_rapiteam.png"

# Configuración de splash screen
# ✅ SOLUCIÓN CORRECTA: Imagen 1152x1152 con espacio alrededor para Android 12+
# Android 12+ recorta el ícono en círculo de 768px de diámetro
flutter_native_splash:
  color: "#FFFFFF"  # Color de fondo blanco
  image: assets/images/1152x1152-rapiteam.png  # Ícono 1152x1152 (especificación oficial)
  android: true
  ios: false  # Deshabilitado porque el proyecto no tiene estructura iOS
  fullscreen: true
  android_12:
    # Android 12+ (API 31+): Recorta en círculo de 768px
    color: "#FFFFFF"
    image: assets/images/1152x1152-rapiteam.png  # 1152x1152 sin fondo de ícono
    icon_background_color: "#FFFFFF"